deployment:
    staging:
        branch: master
        codedeploy:
            githubdemo:
                deployment_group: staging
				deployment_config: CodeDeployDefault.AllAtOnce
				region: us-east-1
				application_root: /